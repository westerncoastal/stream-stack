# 📦 Repomix Repository Snapshot

> Auto-generated by CI
> Branch: **main** | Commit: **74f9db9**
> Generated: 2026-05-03T06:27:01.615Z

---

## 📁 File Overview

### `.github/workflows/deploy.yml`

```text
name: Build, Scan, and Push

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write

    steps:
      # 📥 Checkout
      - name: Checkout
        uses: actions/checkout@v4

      # 🟢 Setup Node
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # 📦 Generate package-lock.json if missing
      - name: Generate lockfile if missing
        run: |
          cd ssai
          if [ ! -f package-lock.json ]; then
            echo "No lockfile found. Generating..."
            npm install
          else
            echo "Lockfile already exists."
          fi

      # 💾 Commit lockfile (only if created)
      - name: Commit package-lock.json
        run: |
          cd ssai
      
          git config user.name "github-actions"
          git config user.email "actions@github.com"
      
          git add package-lock.json
      
          git commit -m "auto-add package-lock.json" || echo "No changes"
      
          git pull --rebase origin main || true
      
          git push origin HEAD:main

      # 🔐 Login to GHCR
      - name: Login to GHCR
        run: echo "${{ secrets.GITHUB_TOKEN }}" | docker login ghcr.io -u ${{ github.actor }} --password-stdin

      # 🐳 Build image
      - name: Build SSAI Image
        run: |
          docker build \
            -t ghcr.io/westerncoastal/stream-stack/ssai:latest \
            ./ssai

      # 📦 Push image
      - name: Push SSAI Image
        run: |
          docker push ghcr.io/westerncoastal/stream-stack/ssai:latest

      # 🔒 Scan container image
      - name: Scan container image
        uses: aquasecurity/trivy-action@v0.20.0
        with:
          image-ref: ghcr.io/westerncoastal/stream-stack/ssai:latest
          severity: CRITICAL
          exit-code: 1

      # 📦 Repomix
      - name: Pack repo
        uses: yamadashy/repomix/.github/actions/repomix@main
        with:
          output: repomix-output.xml
          compress: true

      # 🧠 Generate docs
      - name: Build Repomix Docs
        run: |
          node scripts/repomix-to-md.js
      
      # 📦 Commit docs (safe)
      - name: Commit docs
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add docs/repomix-summary.md
          git commit -m "docs: update repomix summary" || echo "no changes"
          git push
```

### `payments/server.js`

```text
// 1. Create ad campaign payment
⋮----
// 2. Webhook (THIS is critical)
⋮----
// activate ads in DB / NSQ / etc
⋮----
// 3. Creator payout (Stripe Connect required)
```

### `player/index.html`

```text
&lt;!DOCTYPE html&gt;
&lt;html&gt;
&lt;body&gt;
&lt;video id="video" controls autoplay width="800"&gt;&lt;/video&gt;

&lt;script src="https://cdn.jsdelivr.net/npm/hls.js@latest"&gt;&lt;/script&gt;
&lt;script&gt;
const video = document.getElementById('video');

const stream = "http://YOUR_SERVER_IP:4000/playlist.m3u8";

if (Hls.isSupported()) {
  const hls = new Hls();
  hls.loadSource(stream);
  hls.attachMedia(video);
}
&lt;/script&gt;
&lt;/body&gt;
&lt;/html&gt;
```

### `scripts/repomix-to-md.js`

```text
// scripts/repomix-to-md.js
⋮----
function getGitInfo()
⋮----
function xmlToMarkdown(xml)
⋮----
// very lightweight parser (Repomix XML is structured but not huge)
⋮----
md += f.content.slice(0, 4000); // prevent huge docs
⋮----
function main()
```

### `ssai/Dockerfile`

```text
FROM node:20-alpine

# install streamlink + python (needed for live extraction)
RUN apk add --no-cache python3 py3-pip streamlink

WORKDIR /app

# copy ONLY dependency files first (for caching)
COPY package.json package-lock.json* ./

# install dependencies (fallback if lockfile missing)
RUN if [ -f package-lock.json ]; then \
      npm ci --omit=dev; \
    else \
      npm install --omit=dev; \
    fi

# copy rest of app
COPY . .

# optional: drop root for security
RUN addgroup -S app && adduser -S app -G app
USER app

CMD ["node", "server.js"]
```

### `ssai/package.json`

```text
{
  "name": "ssai",
  "version": "1.0.0",
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "xml2js": "^0.6.2",
    "node-schedule": "^2.1.1",
    "busboy": "^1.6.0"
  }
}
```

### `ssai/server.js`

```text
/**
 * -------------------------
 * STREAM FAILOVER SYSTEM
 * -------------------------
 */
let sources = []; // { url, failCount }
⋮----
const getCurrentSource = ()
⋮----
/**
 * SAFE STREAMLINK EXECUTION
 */
const getLiveUrl = (providerUrl) =&gt;
⋮----
/**
 * VALIDATE URL
 */
const isValidUrl = (url) =&gt;
⋮----
/**
 * FAILOVER MONITOR
 */
⋮----
source.failCount = 0; // reset on success
⋮----
/**
 * ADD STREAM SOURCE
 */
⋮----
/**
 * SAFE UPLOAD
 */
⋮----
/**
 * -------------------------
 * SSAI PLAYLIST (FIXED)
 * -------------------------
 */
⋮----
// inject ad every 5 segments
```

### `ssai/targeting.js`

```text
// ssai/targeting.js
function getUserProfile(req)
⋮----
function chooseAdServer(user)
⋮----
// Use the SERVER_IP injected by the cloud-init script
⋮----
// zones=1 is the default for your first Revive video zone
⋮----
// Basic logic: you can append targeting parameters to the VAST URL
```

### `ssai/tracker.js`

```text
async function fireBeacon(url)
```

### `ssai/vast.js`

```text
async function fetchVastAd(vastUrl)
```

### `.dockerignore`

```text
node_modules
.git
.gitignore
.env
npm-debug.log

# keep lockfile
!package-lock.json
```

### `docker-compose.yml`

```text
version: "3.9"

services:
  streaming:
    image: bluenviron/mediamtx:latest-alpine
    container_name: streaming
    ports:
      - "1935:1935"
      - "8888:8888"
    volumes:
      - ./hls:/hls
    restart: unless-stopped

  ssai:
    image: ghcr.io/westerncoastal/stream-stack/ssai:latest
    container_name: ssai
    depends_on:
      - streaming
    environment:
      - SERVER_IP=${SERVER_IP}
    volumes:
      - ./hls:/hls
      - ./ads:/ads
    ports:
      - "4000:4000"
    restart: unless-stopped

  revive-db:
    image: mariadb:10.6
    container_name: revive-db
    environment:
      MYSQL_DATABASE: revive
      MYSQL_USER: revive
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - ./mysql:/var/lib/mysql
    restart: unless-stopped

  revive:
    image: reviveadserver/revive-adserver:latest
    container_name: revive
    depends_on:
      - revive-db
    ports:
      - "8081:80"
    volumes:
      - ./revive_conf:/var/www/html/var
    restart: unless-stopped
```

### `nginx.conf`

```text
worker_processes auto;

events { worker_connections 1024; }

rtmp {
    server {
        listen 1935;

        application live {
            live on;

            hls on;
            hls_path /tmp/hls;
            hls_fragment 2;
        }
    }
}

http {
    server {
        listen 80;

        location /hls {
            root /tmp;
        }

        location / {
            root /usr/share/nginx/html;
        }
    }
}
```

### `setup.sh`

```text
#!/bin/bash
set -e

# --- 1. GET KEYS ---
if [ -z "$STRIPE_SECRET_KEY" ]; then
    read -p "Enter Stripe Secret Key (sk_live_...): " STRIPE_SECRET_KEY
fi

if [ -z "$STRIPE_WEBHOOK_SECRET" ]; then
    read -p "Enter Stripe Webhook Secret (whsec_...): " STRIPE_WEBHOOK_SECRET
fi

# --- 2. GET SERVER IP ---
SERVER_IP=$(curl -s https://api.ipify.org)

# --- 3. INSTALL DOCKER ---
apt update && apt install -y docker.io docker-compose-plugin git
systemctl enable --now docker

# --- 4. CLONE REPO ---
rm -rf /opt/stack
git clone https://github.com/westerncoastal/stream-stack.git /opt/stack
cd /opt/stack

# --- 5. CREATE ENV FILE ---
cat &lt;&lt;EOF &gt; .env
DOMAIN=$SERVER_IP
SERVER_IP=$SERVER_IP
STRIPE_KEY=$STRIPE_SECRET_KEY
STRIPE_WEBHOOK_SECRET=$STRIPE_WEBHOOK_SECRET
MYSQL_PASSWORD=$(openssl rand -hex 12)
MYSQL_ROOT_PASSWORD=$(openssl rand -hex 12)
EOF

chmod 600 .env

# --- 6. PATCH PLAYER ---
if [ -f "player/index.html" ]; then
    sed -i "s/YOUR_SERVER_IP/$SERVER_IP/g" player/index.html
fi

# --- 7. START SERVICES ---
docker compose pull
docker compose up -d

# --- 8. AUTO UPDATE SERVICE ---
cat &lt;&lt;EOF &gt; /usr/local/bin/update-stack.sh
#!/bin/bash
cd /opt/stack || exit 1
git fetch origin main
git reset --hard origin/main
docker compose pull
docker compose up -d
EOF

chmod +x /usr/local/bin/update-stack.sh

# run every minute
(crontab -l 2&gt;/dev/null; echo "*/1 * * * * /usr/local/bin/update-stack.sh") | crontab -

echo "✅ Done! Access your player at http://$SERVER_IP"
```

